import { Meta } from '@storybook/blocks'

<Meta title="Organisms/Header" />

# Header

The header component will be displayed in every application route.

Its functionality is to allow the user navigate in the application, and search for the blog posts.
