import { Meta } from '@storybook/blocks'

<Meta title="Atoms/Button" />

# Button

The Button component should be used in the whole application, it is the main component for the user to interact with the application.
