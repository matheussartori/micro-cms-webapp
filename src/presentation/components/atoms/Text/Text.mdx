import { Meta } from '@storybook/blocks'

<Meta title="Atoms/Text" />

# Text

The text component should be used in every part of the application, instead of using a simple ```<p>``` tag, for example.

The sizes of the text are fixed, to make the whole application font sizes more consistent.
